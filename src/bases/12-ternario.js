const activo = true;
// Condicion if, else.
// let mensaje = "";

// if (activo) {
//   mensaje = "Activo";
// } else {
//   mensaje = "Inactivo";
// }

// operador ternarios
// const mensaje = activo ? "Activo" : "Inactivo";
// const mensaje = activo ? "Activo" : null;
// Condicion AND
const mensaje = activo && "Activo"; // para cuando necesitamos hacer algo si la condicion se cumple y no hacer nada en caso contrario

console.log(mensaje);
